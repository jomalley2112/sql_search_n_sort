- @mode = :sort
%form
	= hide_current_params("sort_by").html_safe
	= label_tag :sort_by
	= select_tag :sort_by, options_for_select(opts ||= @sort_dropdown_opts ||= [], @selected_sort_opt), :onchange => "submit();"