%h4 Listing People
%table
	%thead
		%tr
			%td.sort-by{:colspan => 6, :style => "padding-bottom: 15px;"}
				-hidden_inputs_str = "" #hidden_field_tag :query, params[:query]
				=render :partial => "application/sort_form", :locals => {:form_action => people_path, :opts => @sort_opts, :hidden_inputs => hidden_inputs_str}
		
		%tr
			%th First name
			%th Last name
			%th Email
			%th Age
			%th Last updated
			%th
	%tbody
	- @people.each do |person|
		%tr
			%td= person.first_name
			%td= person.last_name
			%td= person.email
			%td= person.age
			%td= person.updated_at
			%td= link_to "Edit", edit_person_path(person)