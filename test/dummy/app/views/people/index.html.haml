%h4 Listing People
%table
	%thead
		%tr
			%td{:colspan => 3, :style => "padding-bottom: 15px;"}
				-hidden_inputs_str = "" #hidden_field_tag :query, params[:query]
				=render :partial => "application/sort_form", :locals => {:form_action => people_path, :opts => @sort_dropdown_opts, :hidden_inputs => hidden_inputs_str}
			%td{:colspan => 3, :style => "padding-bottom: 15px;"}
				=render :partial => "application/search_form", :locals => {:form_action => people_path, :hidden_inputs => ""}
		%tr
			%th First name
			%th Last name
			%th Email
			%th Age
			%th Last updated
			%th
	%tbody
	- @people.each do |person|
		%tr
			%td= person.first_name
			%td= person.last_name
			%td= person.email
			%td= person.age
			%td= person.updated_at
			%td= link_to "Edit", edit_person_path(person)